import React from 'react'
import { SafeAreaView, Text } from 'react-native'

const App = (): React.ReactElement => {
  return (
    <SafeAreaView>
      <Text>Welcome</Text>
    </SafeAreaView>
  )
}

export default App
